#include <iostream>
#include <string>
using namespace std;

int main()
{
	//step 1: initialize the variables for this calendar
	//step 2: determine the final days in the given month by user.
	//step 2.1. convert the letter in the string.
	//step 2.2. compare the month and find the correct days
	//step 3: determine how many rainy day, sunny day and cloudy day in that month
	//step 4: display the result
	
	return 0;
}
